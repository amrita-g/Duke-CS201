import java.util.*;
public class Neargrams {
    public int numOfNeargrams(String words) {
      //split up the string by spaces into an array
      //make a hashset of all the strings to avoid any repititions
      // make 2 for loops
      //first for loop loops through the array - make a map of it, map +1, map -1
      //second for loop loops through the next element of the array - make a new map (3 maps)
      //use .equals to check if any of the maps are equal. if they are equal then count++;
        return 2;
    }
  }